# routes/music/ - Music Section

## Files
- `index.tsx` — MusicIndexPage: music library landing with stats and recent tracks

## Route
- `/music` → MusicIndexPage (child of MusicLayout)
- All music sub-routes (`/music/tracks`, `/music/albums`, etc.) are nested under MusicLayout in `main.tsx`
