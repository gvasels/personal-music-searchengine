/**
 * useSelection Hook - Re-export from SelectionContext
 * Provides access to selection state and actions for batch operations
 */
export {
  useSelection,
  useSelectionOptional,
  type SelectionContextValue
} from '@/lib/context/SelectionContext';
