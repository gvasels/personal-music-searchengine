# LocalStack Development Configuration
# Copy this file to .env.local and fill in values from init-cognito.sh output
#
# Usage:
#   1. Start LocalStack: docker-compose -f docker/docker-compose.yml up -d
#   2. Run init scripts: ./docker/localstack-init/init-aws.sh && ./docker/localstack-init/init-cognito.sh
#   3. Copy values from init-cognito.sh output below
#   4. Start frontend: npm run dev:local

# Enable LocalStack mode
VITE_LOCAL_STACK=true

# Backend API URL (local Go server)
VITE_API_URL=http://localhost:8080

# Cognito Configuration
# Get these values from init-cognito.sh output:
#   User Pool ID:  us-east-1_xxxxxxxxx
#   Client ID:     xxxxxxxxxxxxxxxxxxxxxxxxxx
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=

# LocalStack Cognito endpoint
VITE_COGNITO_ENDPOINT=http://localhost:4566

# AWS Region
VITE_COGNITO_REGION=us-east-1

# ============================================
# Test User Credentials (for manual testing)
# ============================================
# Email: admin@local.test      Password: LocalTest123!  (admin group)
# Email: subscriber@local.test Password: LocalTest123!  (subscriber group)
# Email: artist@local.test     Password: LocalTest123!  (artist group)
